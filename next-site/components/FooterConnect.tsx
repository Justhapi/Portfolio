// UNUSED — no import references anywhere in the codebase (grep verified).
// Safe to delete manually. The sandbox couldn't `rm` it, so this stub
// replaces the file's contents to remove it from the type-check surface
// area and make its unused status obvious in the file listing.
export {};
